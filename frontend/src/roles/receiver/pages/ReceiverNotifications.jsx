import PageHeader from '../../../components/PageHeader';
import PlaceholderSection from '../../../components/PlaceholderSection';

export default function ReceiverNotifications() {
  return (
    <div className="page-stack">
      <PageHeader title="Receiver Notifications" description="Receiver notification space for future updates." />
      <PlaceholderSection title="Notifications" description="Notification items will be added later." />
    </div>
  );
}