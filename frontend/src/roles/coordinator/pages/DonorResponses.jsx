import PageHeader from '../../../components/PageHeader';
import PlaceholderSection from '../../../components/PlaceholderSection';

export default function DonorResponses() {
  return (
    <div className="page-stack">
      <PageHeader title="Donor Responses" description="Donor response tracking will appear here later." />
      <PlaceholderSection title="Responses" description="Response summaries will be added later." />
    </div>
  );
}