import PageHeader from '../../../components/PageHeader';
import PlaceholderSection from '../../../components/PlaceholderSection';

export default function CoordinatorNotifications() {
  return (
    <div className="page-stack">
      <PageHeader title="Coordinator Notifications" description="Coordinator notification space for future updates." />
      <PlaceholderSection title="Notifications" description="Notification items will appear here later." />
    </div>
  );
}